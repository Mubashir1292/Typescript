//! handling the objects with different examples

function printingUser({name,email}:{name:string,email:string}){
    console.log(name,email);
}printingUser({name:"asdljdas",email:"kjhjkhkj"});

//! we are working on objects. 
//? type Aliases

type user={
    id:number,
    name:string,
    isActive:boolean
}



//? we are currently working onto the some working
export{}    
